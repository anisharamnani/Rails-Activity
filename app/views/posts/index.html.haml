%h1 Listing posts

%table
  %tr
    %th Body
    %th Title
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= post.body
      %td= post.title
      %td= link_to 'Show', post
      %td= link_to 'Edit', edit_post_path(post)
      %td= button_to 'Destroy', post, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Post', new_post_path